[package]
name = "circom-witnesscalc"
version = "0.3.0"
edition = "2021"
repository = "https://github.com/iden3/circom-witnesscalc"
authors = ["Oleh Lomaka <oleg.lomaka@gmail.com>"]
description = "Witness calculator for Circom circuits"
license = "MIT OR Apache-2.0"

[features]
default = []
print_opcode = []
debug_vm2 = []
parallel_components = []

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
ark-bn254 = { version = "0.6.0", features = ["std"] }
ark-ff = { version = "0.6.0", features = ["std", "asm"] }
ark-serialize = { version = "0.6.0", features = ["derive"] }
byteorder = "1.4.3"
# Held at 0.8: ark-std 0.6 and ruint both pin rand 0.8, so moving this crate
# alone to 0.10 adds a third copy of rand to the tree rather than removing one.
# Only used by the graph optimizer's random_eval, never on the witness path.
rand = "0.8.5"
ruint = { version = "1.13.1", features = ["rand", "serde", "num-traits"] }
serde = { version = "1.0.190", features = ["derive"] }
serde_json = "1.0.64"
wtns-file = "0.1.5"
libc = "0.2.155"
# circom dependencies
#compiler = { path = "../circom/compiler" }
#code_producers = { path = "../circom/code_producers" }
#program_structure = { path = "../circom/program_structure" }
#parser = { path = "../circom/parser" }
#type_analysis = { path = "../circom/type_analysis" }
#constraint_generation = { path = "../circom/constraint_generation" }
prost = "0.14.4"
# Held at 0.4 for the same reason: ark-ff 0.6 and ark-circom depend on 0.4.
num-bigint = "0.4.8"
num-traits = "0.2.19"
anyhow = "1.0.97"
indicatif = "0.18.6"
memmap2 = "0.9.5"
tempfile = "3.19.1"
winnow = "1.0.4"
thiserror = "2.0.12"
bitvec = "1.0.1"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
#debug = true

[lib]
crate-type = ["lib", "staticlib", "cdylib"]

[build-dependencies]
bindgen = "0.72.0"
prost-build = "0.14.4"

[workspace]
members = ["extensions/build-circuit", "extensions/compiler"]
